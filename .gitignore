# SMTP Configuration (contains sensitive credentials)
smtp_config.php

# PHPMailer (if downloaded manually - not needed if using Composer)
PHPMailer/

# Composer (vendor will be installed on Vercel via composer install)
vendor/
composer.lock

# Node modules (installed on Vercel)
node_modules/
package-lock.json

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

